name: flutter_app_gastos
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  firebase_core: ^2.31.0
  google_fonts: ^6.2.1  # Agregamos la dependencia de Google Fonts
  firebase_auth: ^4.20.0
  cloud_firestore: ^4.17.5
  share: ^2.0.4
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true  # see https://flutter.dev/custom-fonts/#from-packages
  assets:
    - assets/images/